package ca.ciccc.java.model;

import java.util.Collections;
import java.util.Comparator;

/**
 * This comparator class should be used anytime you need to sort Players by
 * points. Players with more points are highest than others.
 */
public class PlayerSortByPointsComparator implements Comparator<Player> {

	@Override
	public int compare(Player p1, Player p2) {
		// 2 Points
		// TODO Write your code here
		Collections.sort(list, int);
		return 0;
	}
}
